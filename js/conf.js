const loginForm = document.getElementById("login-form");
// const msgAlertErroLogin = document.getElementById("msgAlertErroLogin");
// const loginModal = new bootstrap.Modal(document.getElementById("loginModal"));
loginForm.addEventListener("submit", async (e) => {
    e.preventDefault();

});

// login-Form, do formulario index.php
// ESTAMOS TRABALHANDO COM ID POR ISSO O getElementById
//criando o const para erro de login
 // usando para não recarregar a página
// e.preventDefault();